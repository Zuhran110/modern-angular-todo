import { HighligthCompleteTodoDirective } from './highligth-complete-todo.directive';

describe('HighligthCompleteTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthCompleteTodoDirective();
    expect(directive).toBeTruthy();
  });
});
